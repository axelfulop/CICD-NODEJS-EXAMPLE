{
    "name": "node-app",
    "description": "build test",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "express": "^4.17.1"
    },
    "scripts": {
        "start": "node index.js",
        "test": "mocha --timeout 100000 --exit"
    },
    "devDependencies": {
        "mocha": "^7.1.2",
        "supertest": "^4.0.2"
    }
}
